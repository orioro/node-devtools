{
  "name": "@orioro/devtools",
  "private": true,
  "version": "0.0.0",
  "packageManager": "yarn@4.0.2",
  "scripts": {
    "changeset": "changeset",
    "?publish": "changeset does not support yarn@3+. Track: https://github.com/changesets/changesets/issues/432",
    "publish": "yarn workspaces foreach --no-private --all npm publish",
    "build": "yarn workspaces foreach --all --parallel --interlaced --topological-dev run build",
    "claude": "docker compose -f ./packages/template-docker-compose/docker-compose.yml --profile claude up -d && docker exec -it $(docker ps --filter name=claude --format \"{{.Names}}\" | grep -v firewall | head -n 1) zsh"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.2",
    "prettier": "^3.5.2"
  }
}
