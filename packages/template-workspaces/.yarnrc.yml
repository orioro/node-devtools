nodeLinker: node-modules

# Disabled: per-workspace hoisting limits that would allow packages to have
# isolated node_modules with different dependency versions (e.g. next@13 vs next@14).
# Uncomment if version conflicts between workspaces require isolation.
# nmHoistingLimits: workspaces

npmRegistryServer: "https://registry.npmjs.org/"
